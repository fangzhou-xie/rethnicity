# TODO: predict ethnicity with fullname or lastname model

#' @export
predict_ethnicity <- function(name_vector, model = "lastname") {
  
}

predict_ethnicity_fullname <- function(name_vector) {
  # use the full name model
  
}

predict_ethnicity_lastname <- function(name_vector) {
  # use the last name model
  
}
